# Core CV / model
ultralytics>=8.3.0
opencv-python>=4.10.0

# Optimization / edge inference
onnx>=1.16.0
onnxruntime>=1.18.0

# Demo
streamlit>=1.38.0

# Data / utils
huggingface_hub>=0.24.0
matplotlib>=3.9.0
pyyaml>=6.0
pillow>=10.0.0
tqdm>=4.66.0

# NOTE: install the CUDA build of torch/torchvision separately so training
# uses your GPU (not the CPU-only default):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
